import styles from './HeroSection.module.scss';

const HeroSection = () => {
	return (
		<section className={ styles.wrapper }>
			<span>
				Welcome to navigation through <span>EzioMercer</span>'s projects

				<br/>
				<br/>
				<br/>

				↓
			</span>
		</section>
	)
}

export default HeroSection;
